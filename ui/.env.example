# UI jarvis-suit — copier en `.env.local` (jamais committé) pour surcharger.
# Base API du backend jarvis-suit (FastAPI). Le proxy Vite renvoie /api et /ws
# vers cette valeur en dev. Défaut : http://127.0.0.1:8000
VITE_API_BASE=http://127.0.0.1:8000
