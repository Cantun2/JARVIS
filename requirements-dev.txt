# Fallback si uv/pip -e indisponible : `pip install -r requirements-dev.txt`
# Source de vérité = pyproject.toml (extras [dev]). Ce fichier est un filet.
pydantic>=2.7
pydantic-settings>=2.2
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
structlog>=24.1
pytest>=8.0
pytest-asyncio>=0.23
mypy>=1.10
ruff>=0.5
